/**
 * robot-image.js — Robot simulation screenshot asset.
 *
 * This file sets window.RI to the base64-encoded robot image.
 * To update the image:
 *   1. Convert your image to base64: `base64 -i your-image.jpg`
 *   2. Replace the value below with: "data:image/jpeg;base64,<your-base64>"
 *
 * If window.RI is not set, the robot page will show an empty placeholder.
 * The original base64 image from the presentation deck should be pasted here.
 */

// Paste your base64 image string here:
// window.RI = "data:image/jpeg;base64,/9j/4AAQSkZJRg...";

window.RI = null; // set to null to show placeholder
